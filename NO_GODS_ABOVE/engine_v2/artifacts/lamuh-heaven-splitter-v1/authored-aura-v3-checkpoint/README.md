# Historical authored-aura V3 checkpoint

Preserved before the user-requested V4 surrounding-aura sprite redraw was tested.
The 163 top-level evidence files were copied and SHA-256 verified against their
originals. The V3 smoke script and review-data source metadata are included.

`browser-report.json` records the completed V3 test, not V4 approval. Older partial
reports retain their original version/timestamps. The source04 `aura-gather`
filename means the early rising connector; the separate source08
`p1-heavy-hit-post-apex-gather.png` and adjacent JSON prove post-apex aura extinction
for V3 only. V4 intentionally redraws release poses03–08 and retains the previous
clean poses as a comparison fallback, not pixel-identical aura removal.

The procedural-aura checkpoint remains separate in the parent folder. None of
these checkpoints constitutes human motion or production approval.
