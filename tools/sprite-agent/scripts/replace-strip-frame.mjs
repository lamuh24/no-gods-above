#!/usr/bin/env node
import fs from "node:fs/promises";import path from "node:path";import sharp from "../node_modules/sharp/lib/index.js";
const[input,output,targetArg,sourceArg]=process.argv.slice(2),target=Number(targetArg),source=Number(sourceArg);if(!output)throw new Error("Usage: replace-strip-frame.mjs <input> <output> <target> <source>");const m=await sharp(input).metadata(),count=(m.width??0)/448,frames=[];for(let i=0;i<count;i++){const from=i===target?source:i;frames.push({input:await sharp(input).extract({left:from*448,top:0,width:448,height:448}).png().toBuffer(),left:i*448,top:0});}await fs.mkdir(path.dirname(output),{recursive:true});await sharp({create:{width:count*448,height:448,channels:4,background:{r:0,g:0,b:0,alpha:0}}}).composite(frames).png().toFile(output);console.log(output);
